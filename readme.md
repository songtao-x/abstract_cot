# Abstract CoT

## Coundown exp
The prompt is saved in `prompt_template.py`

Main code is in `train/grpo.py`

To run the training, use 
```bash
sh sh/cd_grpo.sh
```

## GSM
Training code can be found in `train/train_rl.py`.

Processing for GSM set can be found in `train/gsm`
